---
title: Checkbox
description: Checkbox allows users to make a binary choice—either toggling a single option on or off or selecting multiple options from a list.
provider: shadcn
features:
  - "Can be controlled or uncontrolled"
  - "Multiple variants, sizes and border radiuses"
  - "Supports indeterminate state"
  - "Handles invalid appearance"
  - "Supports custom icon and styles"
  # - "Respects reduce motion with system settings and through props"
---

<ComponentShowcase name="checkbox-demo" />

## Installation

<Tabs defaultValue="terminal">
  <TabsList>
    <TabsTrigger value="terminal">
      <Terminal className="w-4 h-4" />
      Terminal
    </TabsTrigger>
    <TabsTrigger value="manual">
      <Code2 className="w-4 h-4" />
      Manual
    </TabsTrigger>
  </TabsList>
  <TabsContent value="terminal">
    <InstallationCommands
      type="registry-installation"
      registryPath="pure-ui/components/checkbox-shadcn"
    />
  </TabsContent>
  <TabsContent value="manual">
    <Steps currentStep={1}>
      <Step stepNumber={1} title="Install dependencies">
        <InstallationCommands
          type="package-installation"
          npmCommand="npm install @radix-ui/react-checkbox class-variance-authority clsx"
          pnpmCommand="pnpm add @radix-ui/react-checkbox class-variance-authority clsx"
          yarnCommand="yarn add @radix-ui/react-checkbox class-variance-authority clsx"
          bunCommand="bun add @radix-ui/react-checkbox class-variance-authority clsx"
        />
      </Step>
      <Step
        stepNumber={2}
        title="Add cn function to your utils"
      >
        ```ts
        import { clsx, type ClassValue } from "clsx";
        import { twMerge } from "tailwind-merge";

        export function cn(...inputs: ClassValue[]) {
          return twMerge(clsx(inputs));
        }
        ```
      </Step>
      <Step
        stepNumber={3}
        title="Copy and paste the following code into your project"
        isLast
      >
        <ComponentCodePreview name="checkbox-shadcn" />
      </Step>
    </Steps>

  </TabsContent>
</Tabs>

## Anatomy

```tsx
// Option #1. Basic usage:
<Checkbox />

// Option #2. Custom usage:
<CheckboxRoot>
  <CheckboxIndicator />
</CheckboxRoot>
```

## Usage

### Disabled

<ComponentShowcase name="shadcn-checkbox-disabled-demo" />

### Sizes

<ComponentShowcase name="shadcn-checkbox-sizes-demo" />

### Radius

<ComponentShowcase name="shadcn-checkbox-radius-demo" />

### Custom Icons

<ComponentShowcase name="shadcn-checkbox-icons-demo" />
