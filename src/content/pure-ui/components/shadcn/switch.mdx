---
title: Switch
description: Switch is a toggle control used to turn a single option on or off. Commonly used for binary settings—like enabling features or switching modes—it provides immediate, visible feedback to indicate the current state.
provider: shadcn
features:
  - "Can be controlled or uncontrolled"
  - "Full keyboard navigation"
  - "Multiple sizes, elastic effect"
  - "Animated using Motion"
  - "Supports left, right and thumb icons"
  - "Easy to customize"
  - "Handles invalid appearance"
  - "Respects reduce motion settings"
---

<ComponentShowcase name="switch-demo" />

## Installation

<Tabs defaultValue="terminal">
  <TabsList>
    <TabsTrigger value="terminal">
      <Terminal className="w-4 h-4" />
      Terminal
    </TabsTrigger>
    <TabsTrigger value="manual">
      <Code2 className="w-4 h-4" />
      Manual
    </TabsTrigger>
  </TabsList>
  <TabsContent value="terminal">
    <InstallationCommands
      type="registry-installation"
      registryPath="pure-ui/components/switch-shadcn"
    />
  </TabsContent>
  <TabsContent value="manual">
    <Steps currentStep={1}>
      <Step stepNumber={1} title="Install dependencies">
        <InstallationCommands
          type="package-installation"
          npmCommand="npm install @radix-ui/react-switch motion class-variance-authority clsx"
          pnpmCommand="pnpm add @radix-ui/react-switch motion class-variance-authority clsx"
          yarnCommand="yarn add @radix-ui/react-switch motion class-variance-authority clsx"
          bunCommand="bun add @radix-ui/react-switch motion class-variance-authority clsx"
        />
      </Step>
      <Step
        stepNumber={2}
        title="Add cn function to your utils"
      >
        ```ts
        import { clsx, type ClassValue } from "clsx";
        import { twMerge } from "tailwind-merge";

        export function cn(...inputs: ClassValue[]) {
          return twMerge(clsx(inputs));
        }
        ```
      </Step>
      <Step
        stepNumber={3}
        title="Copy and paste the following code into your project"
        isLast
      >
        <ComponentCodePreview name="switch-shadcn" />
      </Step>
    </Steps>

  </TabsContent>
</Tabs>

## Anatomy

```tsx
// Option #1. Basic usage:
<Switch />

// Option #2. Custom usage:
<SwitchRoot>
  <SwitchThumb />
</SwitchRoot>
```

## Examples

### Size

<ComponentShowcase name="shadcn-switch-size-demo" />

### Elastic

<ComponentShowcase name="shadcn-switch-elastic-demo" />

### Reduce Motion

<ComponentShowcase name="shadcn-switch-reduce-motion-demo" />

### Disabled

<ComponentShowcase name="shadcn-switch-disabled-demo" />

### Thumb Icon

<ComponentShowcase name="shadcn-switch-thumb-icon-demo" />

### Icons

<ComponentShowcase name="shadcn-switch-icons-demo" />
