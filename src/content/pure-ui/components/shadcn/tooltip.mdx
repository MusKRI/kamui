---
title: Tooltip
description: Tooltip is a popup that displays information when hovering over an element.
provider: shadcn
---

<ComponentShowcase name="tooltip-demo" />

## Installation

<Tabs defaultValue="terminal">
  <TabsList>
    <TabsTrigger value="terminal">
      <Terminal className="w-4 h-4" />
      Terminal
    </TabsTrigger>
    <TabsTrigger value="manual">
      <Code2 className="w-4 h-4" />
      Manual
    </TabsTrigger>
  </TabsList>
  <TabsContent value="terminal">
    <InstallationCommands
      type="registry-installation"
      registryPath="pure-ui/components/tooltip-shadcn"
    />
  </TabsContent>
  <TabsContent value="manual">
    <Steps currentStep={1}>
      <Step stepNumber={1} title="Install dependencies">
        <InstallationCommands
          type="package-installation"
          npmCommand="npm install @radix-ui/react-tooltip motion class-variance-authority clsx"
          pnpmCommand="pnpm add @radix-ui/react-tooltip motion class-variance-authority clsx"
          yarnCommand="yarn add @radix-ui/react-tooltip motion class-variance-authority clsx"
          bunCommand="bun add @radix-ui/react-tooltip motion class-variance-authority clsx"
        />
      </Step>
      <Step
        stepNumber={2}
        title="Add cn function to your utils"
      >
        ```ts
        import { clsx, type ClassValue } from "clsx";
        import { twMerge } from "tailwind-merge";

        export function cn(...inputs: ClassValue[]) {
          return twMerge(clsx(inputs));
        }
        ```
      </Step>
      <Step
        stepNumber={3}
        title="Copy and paste the following code into your project"
        isLast
      >
        <ComponentCodePreview name="tooltip-shadcn" />
      </Step>
    </Steps>

  </TabsContent>
</Tabs>

## Anatomy

```tsx
<Tooltip>
  <TooltipTrigger />
  <TooltipContent />
</Tooltip>
```

## Animation Presets

### Motion

<ComponentShowcase name="tooltip-motion-demo" />

### Wipe

<ComponentShowcase name="tooltip-wipe-demo" />

### Slide

<ComponentShowcase name="tooltip-slide-demo" />

### Scale

<ComponentShowcase name="tooltip-scale-demo" />
